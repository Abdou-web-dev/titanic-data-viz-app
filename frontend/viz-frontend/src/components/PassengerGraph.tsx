import { FunctionComponent } from "react";
import { Passenger } from "../types/types";

interface PassengerGraphProps {
  passengers: Passenger[];
}

const PassengerGraph: FunctionComponent<PassengerGraphProps> = () => {
  return <></>;
};

export default PassengerGraph;
